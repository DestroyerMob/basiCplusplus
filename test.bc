// Representative BasicC source for the lexer milestone.
function maximum(left: float, right: double) -> float:
    variable result = left;

    if right > left:
        result = right; // Choose the larger value.

    return result;
